// src/hooks/index.ts

export { useNews, useNewsArticle } from './useNews';
export { useLocation } from './useLocation';
export { useMapData } from './useMapData';
export { useHub } from './useHub';

// Note: usePrivacy is in src/contexts/PrivacyContext.tsx
// Note: useTheme is in src/contexts/ThemeContext.tsx
// Import them directly from @/contexts when needed